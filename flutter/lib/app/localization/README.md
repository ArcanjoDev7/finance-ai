# Localização

As mensagens serão adicionadas em arquivos ARB (`app_pt.arb` e `app_en.arb`) quando a primeira interface for criada. Nenhuma string de interface deve ser considerada fonte de verdade fora desses arquivos.
